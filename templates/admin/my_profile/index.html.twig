{# templates/admin/site_settings/index.html.twig #}
{% extends '@EasyAdmin/page/content.html.twig' %}
{% block page_title %}{{ page_title }}{% endblock %}
{% block content_title %}{{ page_title }}{% endblock %}
{% block main %}
    <div class="profile">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="mb-2 w-100 btn btn-lg btn-primary" type="submit">{{ button_label|default( 'Save'|trans ) }}</button>
        {{ form_end(form) }}
    </div>
{% endblock %}
